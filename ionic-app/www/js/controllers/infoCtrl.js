app_controllers.controller('InfoCtrl', ['$scope', '$rootScope', 'DrupalNodesService', 'drupal_instance', function ($scope, $rootScope, DrupalNodesService, drupal_instance) {
/* var endpoint = drupal_instance.baseUrl;
  var infoResource = drupal_instance.resources.info;

  var requestObject = {
    method        : 'GET',
    url           : endpoint + infoResource,
    dataType      : 'json',
    crossDomain   : true
  };

  // Get All Sessions
  DrupalNodesService.all(requestObject).then(
    function (response) {
      $scope.info = response;
    },
    function (error) {
      $scope.error = error;
    }
  );*/
}]);

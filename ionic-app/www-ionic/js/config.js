var app_config = angular.module('drupal8-ionic.configuration', []);

/**
 * REPLACE HERE YOUR DRUPAL INSTANCE URL AND API ENDPOINT
 **/

app_config.constant('drupal_instance', {
  baseUrl: 'http://ddd',
  servicesEndPoint: '',
});
